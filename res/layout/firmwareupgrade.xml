<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#3c4953"
    tools:context=".FirmwareUpgrade" >

    <TextView
        android:id="@+id/developer_trigger"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:text="Module Cloud Sample App"
        android:gravity="center"
        android:layout_weight="1"
        android:textColor="#e5ffffff"
        android:textSize="20sp"
        android:background="#2d3840" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:focusableInTouchMode="true"
        android:layout_weight="10.3"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin" >

        <Button
            android:id="@+id/firmware_download"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="sendfirmwaredownload"
            android:text="Firmware Download"
            android:background="@drawable/btn_submit_du"
            android:textColor="#e5ffffff"
            android:textSize="16sp"
            android:visibility="gone" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="sendfirmwarecheck"
            android:text="Firmware Check"
            android:background="@drawable/btn_submit_du"
            android:textColor="#e5ffffff"
            android:textSize="16sp"
            android:layout_marginTop="3dp" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="sendfirmwareupgrade"
            android:text="Firmware Upgrade"
            android:background="@drawable/btn_submit_du"
            android:textColor="#e5ffffff"
            android:textSize="16sp"
            android:layout_marginTop="3dp" />

    </LinearLayout>

</LinearLayout>

